// Admin credentials — stored here only as per project requirement
// Change these before deploying to production
export const ADMIN_CONFIG = {
  username: "admin",
  password: "admin123",
} as const;
